# her datasets land here — see `forge/datasets.py`

Reference data must cite sources: one source is a *lead*, two independent
sources make a *fact*. The included example, **`rocket_materials`** (melting
points of rocket-relevant metals), is corroborated across three sources — the
`_catalog.json` entry records them and marks it `corroborated: true`.
